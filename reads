install environ - pip install django-environ
toogle debug mode accordingly

configure postgress db:
pip install dj-database-url
pip install psycopg2-binary

STARTING SERVER #daphne

daphne a_core.asgi:application -b 0.0.0.0 -p $PORT